global:
  scrape_interval: 5s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'fraud-api'
    metrics_path: /metrics
    static_configs:
      - targets: ['api:8000']

rule_files:
  - /etc/prometheus/alerts.yml
