server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;

  location /stats/ {
    proxy_pass http://stats:8001/stats/;
    proxy_set_header Host $host;
  }

  location / {
    try_files $uri /index.html;
  }
}
